# Private Libraries

This directory is reserved for future project-specific PlatformIO libraries.

当前固件没有本地私有库。外部依赖在 `Firmware/platformio.ini` 中声明：

- Adafruit NeoPixel
- WiFiManager

The current firmware has no private local libraries. External dependencies are declared in `Firmware/platformio.ini`.
